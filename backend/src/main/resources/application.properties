spring.application.name=request-support-system

# Database Configuration
# H2 Database (Default - for development)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/request_support_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=881985Ae
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Custom File Upload Settings
# Attachment settings
app.upload.attachment.dir=uploads/attachments/
app.upload.attachment.allowed-extensions=pdf,png,jpg,jpeg,docx

# Avatar settings
app.upload.avatar.dir=uploads/avatars/
app.upload.avatar.allowed-extensions=png,jpg,jpeg,gif,webp
app.upload.avatar.max-size=5242880

# JWT Configuration
jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Spring Security OAuth2 Resource Server
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080
